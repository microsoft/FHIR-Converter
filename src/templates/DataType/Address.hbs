{
    "use": "{{addr.use}}",
    "line": [
       	{{#each (toArray addr.streetAddressLine)}}
           	"{{this}}",
       	{{/each}}
    ],
    "city": "{{addr.city}}",
    "state": "{{addr.state}}",
    "country": "{{addr.country}}",
    "postalCode": "{{addr.postalCode}}",
},
