{{#if (eq code 'HP')}},
	"Home"
{{else if (eq code 'H')}},
	"Home"
{{else if (eq code 'WP')}},
	"Work"
{{else if (eq code 'TMP')}},
	"Temporary"
{{else if (eq code 'BAD')}},
	"Old/Incorrect"
{{else if code}},
	"Temporary"
{{/if}}