{
  {{#if raceCode}}
      "url" : "http://hl7.org/fhir/us/core/StructureDefinition/us-core-race",
      "extension" : [
          {{#each (toArray raceCode)}}
              {
                  {{#if (or (eq this.code '1002-5') 
                            (eq this.code '2028-9') 
                            (eq this.code '2054-5')
                            (eq this.code '2076-8')
                            (eq this.code '2106-3'))}}
                      "url" : "ombCategory",
                  {{else}}
                      "url" : "detailed",
                  {{/if}}                   
                      "valueCoding" : {
                          "system":"urn:oid:{{this.codeSystem}}",
                          "code":"{{this.code}}",
                          "display":"{{this.displayName}}",
                      },
              },
          {{/each}}
          {
              "url": "text",
              {{#if (gt (length (toArray raceCode)) 1)}}
                  "valueString":"Mixed",
              {{else}}
                  "valueString":"{{raceCode.displayName}}",
              {{/if}}
          }
      ],                        
  {{/if}}
}
