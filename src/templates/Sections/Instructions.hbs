        {{#with (getFirstCdaSectionsWithExactMatch msg 'Instructions')}}
        	{{#each (toArray Instructions.entry) as |commEntry|}}
            	{{>Resources/Communication.hbs communicationEntry=commEntry.act ID=(generateUUID (toJsonString commEntry.act))}} 
                {{>References/Communication/subject.hbs ID=(generateUUID (toJsonString planEntry.observation)) REF=(concat 'Patient/' (generateUUID (toJsonString ../../msg.ClinicalDocument.recordTarget.patientRole)))}}
            {{/each}}
        {{/with}}