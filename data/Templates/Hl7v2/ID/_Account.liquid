{% if CX.1 and CX.4 -%}
{% capture segment -%}{{CX.1.Value}},{{CX.4.Value}}{% endcapture -%}
{% else -%}
{% capture segment -%}{{CX.Value}}{% endcapture -%}
{% endif -%}
{% capture input -%}{{segment | generate_id_input: 'Account', false}}{% endcapture -%}
{{ input | generate_uuid }}