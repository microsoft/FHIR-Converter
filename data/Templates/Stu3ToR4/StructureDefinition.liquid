{% mergeDiff msg -%}
{
  "contained" : [ {{msg.contained | to_array | batch_render : 'Resource', 'msg'}} ],
  "fhirVersion" : "4.0.1",
  "context":[
    {% for c in msg.context -%}
    {
      "expression" : {{c}}
    },
    {% endfor -%}
  ],
  "contextType" : ""
}
{% endmergeDiff -%}