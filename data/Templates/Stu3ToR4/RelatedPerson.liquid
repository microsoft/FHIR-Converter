{% mergeDiff msg -%}
{
  "contained" : [ {{msg.contained | to_array | batch_render : 'Resource', 'msg'}} ],
  "relationship" : [
    {{msg.relationship | to_json_string | default : '""'}}
  ],
}
{% endmergeDiff -%}