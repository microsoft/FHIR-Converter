{% mergeDiff msg -%}
{
  "encounter" : [{{msg.encounter | to_json_string | default : '""'}}],
  "related" : [ 
    {% for r in msg.related %}
      {% if r.ref %}
        {{ r.ref | to_json_string | default : '""' }}
      {% endif %}
    {% endfor %}
   ]
}
{% endmergeDiff -%}