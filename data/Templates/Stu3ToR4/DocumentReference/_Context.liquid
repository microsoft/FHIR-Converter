{% mergeDiff msg -%}
{
  "related" : [ 
    {% for r in msg.related %}
      {% if r.ref %}
        {{ r.ref | to_json_string | default : '""' }}
      {% endif %}
    {% endfor %}
   ]
}
{% endmergeDiff -%}