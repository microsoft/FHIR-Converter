{% mergeDiff msg -%}
{
  {% if msg.answer == null -%}
  "answerBoolean" : {{msg.hasAnswer}},
  "operator" : "exists",
  {% else -%}
  "operator" : "=",
  {% endif -%}
  "hasAnswer" : "",
  "answerAttachment" : "",
  "answerUri" : ""
}
{% endmergeDiff -%}