{% case code -%}
    {% when "aborted " -%}
        "stopped",
    {% when "active" -%}
        "in-progress",
    {% when "completed" -%}
        "completed",    
    {% when "suspended" -%}
        "on-hold",
    {% else -%}
        "",
{% endcase -%}