{% case code -%}
    {% when "active" -%}
        "active",
    {% when "aborted" -%}
        "resolved",
    {% when "completed" -%}
        "resolved",
    {% when "suspended" -%}
        "inactive",
    {% else -%}
        "{{ code }}",
{% endcase -%}