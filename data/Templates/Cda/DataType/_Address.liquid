"use": {% include 'ValueSet/AddressUse' code: Address.use -%},
"line": [
    {% assign lines = Address.streetAddressLine | to_array -%}
    {% for l in lines -%}
        "{{l._}}",
    {% endfor -%}
],
"city": "{{Address.city._}}",
"state": "{{Address.state._}}",
"country": "{{Address.country._}}",
"postalCode": "{{Address.postalCode._}}",