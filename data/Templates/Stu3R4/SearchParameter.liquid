{% mergeDiff msg -%}
{
  "component" : [
    {%for item in msg.component -%}
    {
      "definition" : {% include 'Shared/Reference2Canonical' msg: item.definition -%},
      "expression" : "{{item.expression}}"
    },
    {% endfor -%}
  ]
}
{% endmergeDiff -%}