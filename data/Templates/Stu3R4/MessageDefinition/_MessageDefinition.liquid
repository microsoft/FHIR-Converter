{% mergeDiff msg -%}
{
  "contained" : [ {{msg.contained | to_array | batch_render : 'Resource', 'msg'}} ],
  "identifier" : [{{msg.identifier | to_json_string }}],
  "eventCoding" : {{msg.event | to_json_string}},
  "base" : {% include 'Shared/Reference2Canonical' msg : msg.parent -%},
  "parent" : {% include 'Shared/Reference2Canonical' msg : msg.parent -%},
  "replaces" : [{{msg.replaces | to_array | batch_render : 'Shared/Reference2Canonical', 'msg' }}],
  "focus" : [{{msg.focus | to_array | batch_render : 'MessageDefinition/Focus', 'msg' }}],
  "allowedResponse" : [{{msg.allowedResponse | to_array | batch_render : 'MessageDefinition/AllowedResponse', 'msg' }}],
  {% if msg.responseRequired == true -%}
  "responseRequired" : "always",
  {% else -%}
  "responseRequired" : "never",
  {% endif -%}
  "event" : ""
}
{% endmergeDiff -%}