{% mergeDiff msg -%}
{
  "content" : [ 
    {% for c in msg.content %}
      {% if c.pReference %}
        {{c.pReference | to_json_string}},
      {% endif %}
    {% endfor %}
   ]
}
{% endmergeDiff -%}