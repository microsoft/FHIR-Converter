{% mergeDiff msg -%}
{
  "contained" : [ {{msg.contained | to_array | batch_render : 'Resource', 'msg'}} ],
    "category" : [
        {{msg.category | to_json_string | default : '""' }},
    ]
}
{% endmergeDiff -%}